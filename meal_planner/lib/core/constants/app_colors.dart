import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette (Based on the new UI)
  static const Color primary = Color(0xFF1AA27F); // The Teal color
  static const Color primaryLight = Color(0xFFE8F5F1);
  static const Color background = Color(0xFFF8F9FA); // Off-white/light gray
  static const Color surface = Colors.white;
  static const Color card = Colors.white;
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  
  // Nutrients
  static const Color protein = Color(0xFF3B82F6); // Blue
  static const Color carbs = Color(0xFFF59E0B);  // Orange
  static const Color fats = Color(0xFFEF4444);   // Red
  
  // State Colors
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  
  static const Color divider = Color(0xFFE5E7EB);
}
